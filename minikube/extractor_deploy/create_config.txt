##both the config and secrets are created using kubectl
secrete created alpaca-creds
kubectl create configmap app-config \
  --from-file=config.yaml=/home/kumararpita/alpaca_stream_ingestion/config/config.yaml
